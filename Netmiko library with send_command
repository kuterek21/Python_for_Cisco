from Netmiko import ConnectHandler

ssh_conn = ConnectHandler(              // you have to specify 4 parts

device_type = "cisco_ios"
hosts = "1.1.1.1"
username = "admin"
password "admin"

)
interface = ssh_conn.send_command("sh ip int brie")  // .send_command("") to make use of viariable ssh_conn
print(interface)
